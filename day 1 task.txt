 1  HTTP1.1 was developed in the year 1997 and HTTP2 was developed in the year 2015.
 
 2  HTTP1.1 compresses data by itself. and HTTP2 uses HPACK for data compression.
 
 3  HTTP1.1 was text-based protocol uses plain text to encode and transmit data.
 
 4  HTTP2 was works on the binary protocol as a series of binary codes encode and transmit data rather than plain text.
 
 5  HTTP1.1 was client sends a request to a server, and the server sends a response back to the client.
 
 6  HTTP2 was  different underlying protocol called Secure Remote Protocol 2 (SRP2) establishes a secure connection between a client and a server.
 
 7  HTTP 1.1 cannot handle buffer overflow vulnerabilities due to the lack of sufficient measures.
 
 8  HTTP 2 includes measures to prevent buffer overflow vulnerabilities.
 
 9  HTTP 1.1 does not include any in-built features so the performance 
 
 10  HTTP 2 is designed to be more efficient and performant than HTTP 1.1.HTTP 2 includes several features like multiplexing, binary protocol and header compression.
 